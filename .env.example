# Absolute base URL of the SuperOffer API, including the /api/v1 prefix.
# Development: point straight at the API port.
# Production: leave unset — runtime-config.sh writes public/config.js instead.
NEXT_PUBLIC_SUPER_OFFER_API_URL=http://localhost:3000/api/v1
